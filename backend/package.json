{
  "name": "kobo-clon-backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "install:all": "npm install && cd ../frontend && npm install && cd ../backend",
    "dev:full": "npm install && cd ../frontend && npm install && npm run build && cd ../backend && node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^2.0.0",
    "uuid": "^11.1.0"
  }
}
