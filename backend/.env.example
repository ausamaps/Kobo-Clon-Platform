PORT=4000

# Cuando se conecte la base real, completar esto y reescribir backend/src/db/index.js
# para usar pg/PostGIS en vez del store simulado en JSON.
DATABASE_URL=postgresql://usuario:password@localhost:5432/kobo_clon

# Cuando se conecte el servidor de archivos real, completar esto y reescribir
# backend/src/routes/submissions.js para subir ahi en vez de a backend/uploads.
FILES_SERVER_URL=
